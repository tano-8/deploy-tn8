name: Deploy main

on:
  push:
    branches: [main]
    paths:
      - "domains/tano8/**"
  workflow_dispatch:

concurrency:
  group: deploy-tano8
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
      - name: Checkout source
        uses: actions/checkout@v6
        with:
          repository: tcp2/tano8
          token: ${{ secrets.GH_TOKEN }}
          path: source

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: source/pnpm-lock.yaml

      - name: Install dependencies
        working-directory: source
        run: pnpm install --frozen-lockfile

      - name: Get current week
        id: date
        run: echo "week=$(date +'%Y-%U')" >> $GITHUB_OUTPUT

      - name: Cache Media (Rotating Key)
        uses: actions/cache@v6
        with:
          path: source/apps/tano8/public/media
          key: ${{ runner.os }}-media-tano8-${{ steps.date.outputs.week }}
          restore-keys: |
            ${{ runner.os }}-media-tano8-

      - name: Cache Astro & Vite
        uses: actions/cache@v6
        with:
          path: |
            source/node_modules/.cache
            source/apps/tano8/.astro
          key: ${{ runner.os }}-astro-tano8-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-astro-tano8-

      - name: Build
        working-directory: source
        run: pnpm build:tano8
        env:
          NODE_ENV: production
          NOTION_API_SECRET: ${{ secrets.NOTION_API_SECRET }}
          DATABASE_ID: 65099a4927b74c3ca9c1a95dc993018a

      - name: Report
        run: |
          echo "Files in dist:"
          find source/apps/tano8/dist -type f | wc -l
          echo "Total size:"
          du -sh source/apps/tano8/dist

      - name: Deploy
        run: pnpm dlx wrangler pages deploy source/apps/tano8/dist --project-name=tano8 --branch=main
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}